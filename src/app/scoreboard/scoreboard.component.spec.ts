/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { Scoreboard.Component.TsComponent } from './scoreboard.component.ts.component';

describe('Scoreboard.Component.TsComponent', () => {
  let component: Scoreboard.Component.TsComponent;
  let fixture: ComponentFixture<Scoreboard.Component.TsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ Scoreboard.Component.TsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(Scoreboard.Component.TsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/